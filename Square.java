import java.awt.*;

public class Square extends Rectagle {
    public Square(double side) {
        super(side, side); // Call the constructor of the superclass (Rectangle) with the same value for length and width
    }
}